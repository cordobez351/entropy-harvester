# ENTROP

Browser entropy harvester demo: **rAF** jitter → **SHA-256** pool → optional **copy seed**. Fluid ASCII + serif strip on canvas; mission copy in HTML. Not an OS CSPRNG replacement.

```bash
npm install && npm run dev
```
