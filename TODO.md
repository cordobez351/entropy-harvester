### ENTROP — shortlist

- [ ] Optional Web Worker for hashing under load
- [ ] Per-source toggles + Art / Audit mode (see legacy notes in git history)
- [ ] `measureLineStats` shrink-wrap panel for seed preview width
